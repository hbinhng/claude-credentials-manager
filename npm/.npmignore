# Staging directory for per-platform sub-packages; generated by
# scripts/publish-npm.sh at release time. Must not ship in the main tarball.
platforms/

# Include bin/ in published package (overrides root .gitignore)
!bin/
